/**
 * Crie um JSON com 3 propriedades;
 * Atribua ele a uma variável;
 * Acesse as propriedades imprmindo no console;
 */

let Pessoa = {
  "nome": "Roberto",
  "idade": 40,
  "profissao": "analista"
}

console.log(`Meu nome é ${Pessoa.nome} tenho ${Pessoa.idade}, e trabalho como ${Pessoa.profissao}`);

